# ==============================================================
#  gestflow — requirements.txt
#  Python 3.10.11 | Sin conflictos de versiones
# ==============================================================

# Deep Learning — modelo, capas, callbacks, MobileNetV2,
# TimeDistributed, LSTM, Dropout, BatchNormalization, Softmax
tensorflow==2.15.0
keras==2.15.0
h5py==3.11.0

# Procesamiento numerico — augmentation, tensores,
# operaciones matriciales, time warping, interpolacion
numpy==1.26.4
scipy==1.11.4

# Vision computacional — captura de camara, extraccion
# de frames, resize, BGR/RGB, VideoWriter, overlay
opencv-python==4.9.0.80
pillow==10.3.0

# Machine Learning — StratifiedKFold, classification_report,
# confusion_matrix, accuracy_score, train_test_split
scikit-learn==1.3.2

# Visualizacion — curvas de entrenamiento por fold,
# heatmaps, confusion matrix, graficas loss y accuracy
matplotlib==3.8.4
seaborn==0.13.2

# Control del escritorio Windows — mover cursor, clicks,
# scroll, drag, hotkeys, alt+tab, menu inicio
pyautogui==0.9.54
pygetwindow==0.0.9
pywin32==306
pycaw==20230407
comtypes==1.4.1

# Utilidades — barra de progreso entrenamiento,
# analisis y resumen del dataset, pausa con ESC,
# historial de entrenamiento en CSV
tqdm==4.66.2
pandas==2.1.4
keyboard==0.13.5